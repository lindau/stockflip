# Android UI kit

Four hi-fi mock screens of the StockFlip Android app — Stocks list, Pairs list, Alerts list (with triggered state), and Stock detail. Each is rendered inside a realistic Pixel-style device frame, side-by-side on a canvas.

Open `index.html`.
